## PR Reviewer Assignment Service

#### ⚠️ _Короткая личная ремарка._
Проект выполнен студентом, который в момент выполнения проходил зимнюю сессию в университете. Поэтому часть решения была реализована в сжатые сроки и с использованием "костылей", но при этом я постарался качественно покрыть все основные требования задания и оформить сервис так, чтобы им удобно было пользоваться и проверять. Желаю терпения человеку, который будет это проверять. Для тестирования использовались мок-запросы с помощью Insomnia.
Логические допущения: при попытке поменять ревьюреа, помимо неактивных пользователей, также пропускался поздатель пул реквеста

### Архитектура решения
Язык: Go

БД: PostgreSQL (dockerized)

Принципы:
- Контейнеризация через docker-compose
- Идемпотентность merge
- Минимизация возможных гонок данных путем правильной реализации транзакций и изоляции
- Чистая архитектура

### 🗄️ Структура базы данных

`team`
- хранит команды
- уникальное имя
- используется для группировки пользователей

`user`

- участник команды
- флаг активности is_active
- принадлежит одной команде

`pull_request`

- PR с автором, названием и статусом (OPEN / MERGED)
- после MERGED нельзя менять ревьюверов

`pull_request_reviewer`

- связь PR с ревьюверами
- хранит до двух пользователей на один PR
- обеспечивает быстрый поиск PR, где пользователь назначен ревьювером

### 🐳 Запуск проекта
Требования:
- Docker
- Docker Compose

###Быстрый старт
##### Клонировать репозиторий
```bash
git clone https://github.com/cQu1x/Avito_Internship
```
##### Забилдить проект
```bash
docker-compose -f deploy/docker-compose.yml build app
docker compose -f deploy/docker-compose.yml up -d
```
##### Запустить сервер
```bash
go run cmd/main.go
```


После успешного поднятия:
- сервис доступен на http://localhost:8080
- база PostgreSQL — на localhost:5432

Миграции применяются автоматически при запуске
